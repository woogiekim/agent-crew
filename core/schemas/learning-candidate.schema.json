{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-crew/schemas/learning-candidate.schema.json",
  "title": "agent-crew progressive-learning candidate",
  "description": "A learning-candidate record produced by the Progressive Agent Learning Loop (see core/rules/progressive-learning.md). Records a session/project/global candidate that the analyst and planner may consume as an advisory hint only. A candidate never bypasses TDD, the reviewer stage, or the quality loop.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "candidate_id",
    "source",
    "memory_layer",
    "evidence_refs",
    "promotion_reason",
    "trust_boundary"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "candidate_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for the candidate. Should be human-readable (e.g. 'ctx-break-spacing-2026-06') so that it can be referenced from review logs and analysis notes."
    },
    "source": {
      "type": "string",
      "enum": ["reviewer_finding", "aar_memo", "user_feedback"],
      "description": "Where the candidate originated. reviewer_finding = a NEEDS_CHANGES finding promoted into a candidate. aar_memo = a project-layer After-Action Review memo (see memory-governance.md). user_feedback = an explicit user-stated preference or correction."
    },
    "memory_layer": {
      "type": "string",
      "enum": ["session", "project", "global_candidate"],
      "description": "Maturity level. session = one task's observation. project = repeated, reviewer-approved project convention. global_candidate = awaiting cross-project evidence before promotion to 'global'."
    },
    "evidence_refs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Paths (file:line or task-id artifact paths) that ground the candidate. A candidate with zero evidence_refs is invalid — every claim must be traceable."
    },
    "promotion_reason": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable justification for the candidate's existence and its current maturity level. Should describe what observation it captures and why it deserves recall."
    },
    "trust_boundary": {
      "type": "string",
      "const": "advisory_until_rule_promotion",
      "description": "Machine-enforced statement that the candidate is advisory input only. A candidate cannot become policy until it has been promoted into a managed rule file under core/rules/. This field must be the literal string 'advisory_until_rule_promotion'."
    },
    "promoted_from": {
      "type": "string",
      "description": "Optional. The candidate_id of the lower-maturity candidate that was promoted into this one."
    },
    "superseded_by": {
      "type": "string",
      "description": "Optional. The candidate_id of a newer canonical candidate that has replaced this one. Presence of this field implies the candidate is in the eviction queue."
    },
    "captured_at": {
      "type": "string",
      "description": "Optional ISO-8601 timestamp of when the candidate was first recorded. Useful for lifecycle audits but not required — the loop is content-deterministic."
    }
  }
}
