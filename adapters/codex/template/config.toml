# Codex project-local subagent defaults.
#
# Official Codex subagent guidance recommends keeping direct-child delegation
# enabled while avoiding recursive fan-out unless explicitly needed.
[agents]
max_threads = 6
max_depth = 1
